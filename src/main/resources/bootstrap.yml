spring:
  application:
    name: limits-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: qa
logging:
  level:
    root: INFO
loggin:
  level:
    org:
      springframework: INFO
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka